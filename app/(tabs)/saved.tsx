import React from 'react';
import { SavedItemsScreen } from '@/ui/screens';

/**
 * Saved Tab
 *
 * Shows user's saved reflections, prayers, notes, bookmarks,
 * highlights, and guide conversations.
 */
export default function SavedTab() {
  return <SavedItemsScreen />;
}
