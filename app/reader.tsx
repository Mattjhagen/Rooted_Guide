import React from 'react';
import { BibleReaderScreen } from '@/ui/screens';

/**
 * Bible Reader Screen
 *
 * Entry point for reading Bible chapters with verse-level interaction.
 */
export default function ReaderScreen() {
  return <BibleReaderScreen />;
}
